services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  dynamodb-local:
    image: amazon/dynamodb-local:2.5.4
    # Persisted to a volume rather than -inMemory: an ordinary container
    # restart (crash, `docker compose restart`, host reboot) shouldn't wipe
    # every table's schema and data along with it. `dynamo.py` also
    # self-heals (recreates tables and retries once) if it ever does find
    # them missing — e.g. the volume itself gets removed — but that's a
    # fallback, not the first line of defense.
    #
    # Named volumes are created root-owned; this image's default non-root
    # user can't write the sqlite file into it, so it's run as root here.
    user: root
    command: ["-jar", "DynamoDBLocal.jar", "-sharedDb", "-dbPath", "/home/dynamodblocal/data"]
    volumes:
      - dynamodb-data:/home/dynamodblocal/data
    ports:
      - "8001:8000"

  dynamodb-init:
    build: .
    command: ["python", "-m", "src.init_tables"]
    environment: &gateway_env
      DYNAMODB_ENDPOINT_URL: http://dynamodb-local:8000
      AWS_ACCESS_KEY_ID: local
      AWS_SECRET_ACCESS_KEY: local
      AWS_REGION: us-east-1
      REDIS_URL: redis://redis:6379/0
      PRESENCE_TTL_SECONDS: "45"
      PRESENCE_HEARTBEAT_SECONDS: "20"
      REGISTRY_TTL_SECONDS: "45"
      LOG_LEVEL: INFO
    depends_on:
      - dynamodb-local

  gateway1:
    build: .
    environment:
      <<: *gateway_env
      GATEWAY_ID: gateway1
    depends_on:
      redis:
        condition: service_healthy
      dynamodb-init:
        condition: service_completed_successfully

  gateway2:
    build: .
    environment:
      <<: *gateway_env
      GATEWAY_ID: gateway2
    depends_on:
      redis:
        condition: service_healthy
      dynamodb-init:
        condition: service_completed_successfully

  gateway3:
    build: .
    environment:
      <<: *gateway_env
      GATEWAY_ID: gateway3
    depends_on:
      redis:
        condition: service_healthy
      dynamodb-init:
        condition: service_completed_successfully

  nginx:
    image: nginx:1.27-alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./web:/usr/share/nginx/html:ro
    ports:
      - "8080:80"
    depends_on:
      - gateway1
      - gateway2
      - gateway3

  prometheus:
    image: prom/prometheus:v2.55.1
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    depends_on:
      - gateway1
      - gateway2
      - gateway3

  grafana:
    image: grafana/grafana:11.3.1
    environment:
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
      GF_AUTH_DISABLE_LOGIN_FORM: "false"
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    ports:
      - "3000:3000"
    depends_on:
      - prometheus

volumes:
  dynamodb-data:
